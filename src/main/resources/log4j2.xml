<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n
        </Property>
        <Property name="APP_NAME">spring-redis-blueprint</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender for local development -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- JSON Appender for production (Grafana/Loki/ELK) -->
        <!-- <Console name="JsonConsole" target="SYSTEM_OUT">
            <JsonTemplateLayout eventTemplateUri="classpath:EcsLayout.json">
                <EventTemplateAdditionalField key="service.name" value="${APP_NAME}"/>
                <EventTemplateAdditionalField key="service.environment" value="${env:ENVIRONMENT:-local}"/>
            </JsonTemplateLayout>
        </Console> -->

        <!-- File Appender with rotation -->
        <RollingFile name="RollingFile"
                     fileName="logs/application.log"
                     filePattern="logs/application-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Error-only file appender -->
        <RollingFile name="ErrorFile"
                     fileName="logs/error.log"
                     filePattern="logs/error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="50 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application loggers -->
        <Logger name="com.boris.springredisblueprint" level="debug" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
            <AppenderRef ref="RollingFile"/>
            <AppenderRef ref="ErrorFile"/>
        </Logger>

        <!-- Spring Framework -->
        <Logger name="org.springframework" level="info" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
        </Logger>

        <!-- Hibernate/JPA -->
        <Logger name="org.hibernate" level="warn" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
        </Logger>

        <!-- SQL logging (useful for debugging) -->
        <Logger name="org.hibernate.SQL" level="debug" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
        </Logger>

        <!-- Show SQL parameters -->
        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="trace" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
        </Logger>

        <!-- Redis -->
        <Logger name="org.springframework.data.redis" level="info" additivity="false">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
        </Logger>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="${env:LOG_APPENDER:-Console}"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>
    </Loggers>
</Configuration>
